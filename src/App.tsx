import React from 'react';
import { BrowserRouter } from 'react-router-dom';
import RoutePage from 'routePage';

function App() {
  return (
    <BrowserRouter>
      <RoutePage />
    </BrowserRouter>
  );
}

export default App;
